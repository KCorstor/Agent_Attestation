[
  {
    "id": "demo-setup",
    "runnable": false,
    "title": "Workspace + merchant server",
    "does": "The demo is initialized and prior run artifacts are cleared. The mock merchant server is started and the merchant's 'paid' endpoint is probed to ensure it's live."
  },
  {
    "id": "auction_gate",
    "runnable": false,
    "informational": true,
    "title": "Auction gate (overview)",
    "listDetail": "Concepts",
    "detailLabel": "Overview",
    "does": "auction_gate.mjs is part of this Attstr payment demo integration in this repository—it wires the MPP Tempo agent (agent.mjs) to mppx’s 402/onChallenge flow and the “wait for auction terms” step. It is not from the mppx package; it is source code shipped with this demo.\n\nIt exports createAuctionGate, which builds what you pass into Mppx.create in agent.mjs:\n• gate.fetch — wraps fetch and keeps the last 402 response so it can be merged with the parsed challenge.\n• gate.onChallenge — the function you hand to mppx as onChallenge: gate.onChallenge.\n\nWhen mppx gets a 402, it calls that onChallenge. Inside it we: build the payment bundle, run onBundle (log / optional bundle file), await waitForTerms (in this demo: poll the terms JSON file until it is OK), run onTerms, then return undefined so mppx does its normal job: sign, pay, and retry the GET.\n\nYou don't change mppx's own files. You write your own onChallenge function and hand it to Mppx.create when you build the client."
  },
  {
    "id": "gate_wiring",
    "runnable": false,
    "informational": true,
    "title": "Auction gate in the agent",
    "listDetail": "Code snippets",
    "detailLabel": "Wiring",
    "does": "In agent.mjs you call createAuctionGate({ ... }) once. It returns gate.fetch and gate.onChallenge. You pass those into Mppx.create together with tempo.charge—that is the whole hook-up (see below). You do not edit files inside the mppx package.\n\nThe first snippet is the onChallenge implementation inside createAuctionGate. The second snippet is where the agent plugs gate.fetch and gate.onChallenge into Mppx.create.",
    "snippets": [
      {
        "file": "auction_gate.mjs (inside createAuctionGate)",
        "code": "async function onChallenge(challenge, _helpers) {\n  const bundle = buildPaymentBundle({\n    challenge,\n    capture402: getLast402(),\n  });\n\n  if (onBundle) await onBundle(bundle);\n\n  if (waitForTerms) {\n    const terms = await waitForTerms(bundle);\n    if (onTerms) await onTerms(bundle, terms);\n  }\n\n  return undefined;\n}\n\nreturn { fetch, getLast402, clearLast402, onChallenge };"
      },
      {
        "file": "agent.mjs",
        "code": "const mppx = Mppx.create({\n  polyfill: false,\n  fetch: gate.fetch,\n  methods: [tempo.charge({ account })],\n  onChallenge: gate.onChallenge,\n});"
      }
    ]
  },
  {
    "id": "agent_start",
    "runnable": true,
    "title": "Agent starts checkout",
    "does": "The agent initiates and prints its setup (wallet, merchant URL, etc). It then issues a real mppx.fetch to the merchant.\n\nIn production, bundleOut and termsFile will be the real terms from the merchant, and the bid/funds from the issuing bank. For this demo they are just initialized here in lieu of actual terms and an actual auction."
  },
  {
    "id": "checkout_402",
    "runnable": false,
    "title": "402 + payment bundle (agent)",
    "does": "When mppx invokes onChallenge after the 402, the agent calls buildPaymentBundle, then onBundle runs — this demo phase is that instant (event + optional write to demo-ui-bundle.json), not a second assembly step. The bundle JSON combines the parsed MPP Payment challenge (what to pay — amount, asset, recipient, challenge identifiers, chain context, etc.) with HTTP context (request URL/method, WWW-Authenticate / Payment material). In production you would POST or queue that payload to an auction or credit desk; the file here simulates that handoff.",
    "doesNot": "Does not pay yet; waitForTerms runs next in the same agent process."
  },
  {
    "id": "waiting_terms",
    "runnable": false,
    "title": "Wait for lender / auction terms (agent)",
    "does": "The agent polls MPP_AUCTION_TERMS_FILE until the file exists, parses as JSON, and defaultAcceptTerms returns proceed (not pending/rejected). Those terms are mock data in this repo; in a real system they would be the deal terms returned when an auction selects a winning bid (or the equivalent from your lender flow).",
    "doesNot": "Does not contact a real auction service — the file is supplied locally in the next phase."
  },
  {
    "id": "lender_terms_written",
    "runnable": false,
    "title": "Mock lender delivers terms (orchestrator)",
    "does": "After checkout_402, the dashboard runs release_mock_auction.mjs (same as npm run auction:release for these paths): it reads the saved payment bundle and writes demo-ui-terms.json with mock auction/bank fields. That simulates the winning bid’s terms arriving from the auction side through whatever channel you use in production — webhook, queue, signed URL, etc.",
    "doesNot": "This step is not inside agent.mjs; it unblocks the polling the agent is already doing."
  },
  {
    "id": "terms_accepted",
    "runnable": false,
    "title": "Terms accepted (agent)",
    "does": "After mock terms parse and pass defaultAcceptTerms (e.g. mockAuction.status cleared_for_issuance), the agent logs them. mppx then builds the Tempo credential from the original merchant Payment challenge — the terms JSON gates whether paying is allowed; it is not merged into the cryptographic payment proof.",
    "doesNot": "Mock bank JSON is not the cryptographic credential; settlement follows the protocol challenge."
  },
  {
    "id": "paid",
    "runnable": false,
    "title": "Paid + final HTTP result (agent)",
    "does": "Emitted after the retried GET returns: HTTP status, Payment-Receipt header if present, and a short body preview. Same agent.mjs process end-to-end.",
    "doesNot": "Does not represent mainnet or a real issuer — Moderato / demo server only."
  }
]
